[project]
name = "huygmbot"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot==21.6",
    "psycopg2-binary==2.9.9",
]

[tool.vercel]
entrypoint = "api.webhook:handler"